const input = require('fs').readFileSync('stdin', 'utf8');
var lines = input.split('\n');

//Loop para ler quantas vezes for necessária
for (let i = 0; i < +lines[0]; i++) {
    console.log('gzuz')
}